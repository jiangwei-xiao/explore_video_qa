# 方法首版与证据审计：提交前 Review

日期：2026-09-15。用户要求review当前更改区并提交一笔commit；未授权push。

## 提交范围

- 方法首版A/B/C/D实现、固定配置、额外依赖锁、后台运行与恢复入口、测试及实验协议/报告。
- 独立证据审计模块、50题题面初标、首批窗口配置、匿名复核页、CSV/机制导出、测试与首批报告。
- 中文接口/核心步骤注释约定及文档索引。

不纳入 `scripts/download_video_mme_full.sh` 和 `data/manifests/video_mme_full_filelist.txt`：二者为独立的全量下载准备，不是本次冻结50题实验与证据分析的必要内容；文件和原暂存状态保持不变，不执行下载。

视频、模型、特征、图板、原始问答和输出快照仍位于忽略目录，不纳入Git。

## Review发现与修复

1. **审计一致率未绑定初审版本。** 原实现直接读取当前初审，若用户查看之后助手修订标签，会用错误版本计算一致率。已在页面导出中绑定初审SHA256，导入匹配当前或历史版本；未版本化/版本缺失的用户记录保留，但不计算一致率。补充历史、缺失和旧格式回归测试。
2. **验收状态写死剩余40题。** 原实现会在后续初审增加后继续报告40题未完成。已按实际计数生成状态，并保持原帧初审、输入可读性、用户复核及研究结论各自独立。补充40/3/0题未审测试。

修复限于审计工具，不修改本轮已冻结的选帧公式、模型调用、实验执行代码或结果。

## 验证

- 专用Conda环境运行完整测试：70 passed、1 skipped；GPU特征测试保持显式跳过，不新增模型调用。
- 保留既有短序列小波边界warning；对应参考逻辑一致性测试通过，不把warning隐藏成无异常。
- `pip check`通过；暂存及未暂存diff空白检查通过；方法安装脚本bash语法检查通过。
- V8轻量DOM测试：新页面绑定初审版本，旧/跨版本草稿不被错误标记为新版本，16帧显示及JSON导出通过。当前环境无真实浏览器，不宣称完成真实浏览器视觉验收。
- 核对21个冻结执行代码文件及300条历史问答的SHA256，全部保持原值。
- 拟提交内容为文本代码/文档/配置；大文件、二进制资产和常见凭据模式扫描未发现异常。生成目录仍被Git忽略。

## 非阻断边界与研究状态

当前仅首批10题原帧图板初审，剩余40题未审；384输入只检查关键帧，用户定量一致率未形成。此提交不代表完成全量证据审计。

黑屏高排名、片段覆盖不等于事件覆盖、局部连续事件证据链不足等属于当前原方法已暴露的研究问题。本次保留可复现首版，不为清理更改区而引入过滤阈值、修改λ或替换选帧目标。后续继续基于实际证据分析定位。

Review结论：上述两处审计正确性问题已修复并验证；未发现阻止保存本轮研究实现与审计工具的剩余问题。按用户明确指令提交一笔，不推送。
