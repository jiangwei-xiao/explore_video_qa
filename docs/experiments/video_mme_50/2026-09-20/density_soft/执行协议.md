# 固定四秒软支持联合选帧：50题验证

运行编号：`density_soft_videomme50_20260920_r1`。复用冻结开发50题及历史基线，不测试预留题；单版本，不按结果调参，不提交或推送。

## 唯一改变

固定v1的1 FPS初始扫描、原问题BLIP、Top-16种子、2秒核心连接、±4秒扩窗合并和条件2 FPS补查。原区域权重与每区域最高分种子保护不变。

候选i的软支持L为其与所属最终区域全部原种子的最大截断余弦；差异u为1减去与本区域已选帧的最大截断余弦。区域提出最大L×u候选，全局以原区域权重乘L×u竞争。新旧来源不产生优先级；同分沿用源PTS与候选编号。参照种子固定，不随已选帧扩张。预算上限16，本次50题均核对16唯一帧/3360视觉Token。

## 运行及暂停条件

独立入口`scripts/run_density_soft.py`，隔离包`src/videoqa_density_soft/`。冻结旧运行层的实现副本并复用原扫描、补查、模型、调用账本及局部已验证选择函数；不改旧v1文件或快照。后台setsid/nohup、排他锁，最多8张实际空闲GPU，每卡常驻模型；CPU和解码各2线程。

从原视频独立执行，不使用离线候选缓存作为正式处理输入。每题生成前核对v1上游离散数据一致、分数/视觉特征绝对误差≤1e-6，并检查最终选择与冻结特征重放一致。前5题通过协议/计时验收后自动运行剩余45题；放行不看正确率。

最终问答最多50次，开始失败也占额度；范围分类和查询均0。BF16、SDPA、官方384预处理、原提示词/解析器、贪心最多8新Token、种子2027。新增补查帧只计算纯视觉特征，不计算ITM。答错和解析失败不重跑；OOM、输入漂移、生成不确定或其他运行异常暂停，不自动重试。已完成答案恢复只校验不重复生成。

## 交付与解释

保存逐题候选、特征、软支持/重复度/区域竞争轨迹、实际16帧图板、原始输出、计分、调用账本和指纹。直接E2E从模型就绪后的原视频处理开始至解析结束；上游核验开销包含并单列，历史跨批次时延仅描述比较。

主要比较软折中与Density-v1，另列Top-K、均匀、B、C-local、R。报告总体/时长准确率、Wilson区间、配对得失及分层bootstrap（10000次，2027）、平均/P50/P90/P95时延和显存。重点复核383-2是否保留船图、103-1原线索及所有新增退化；不把选帧改善直接等同于回答改善。50题均已被观察，不当作独立确认集。
