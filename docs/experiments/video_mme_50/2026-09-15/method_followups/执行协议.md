# SB-1.3与SB-1.3-exp01｜第一轮方法改进验证：C-local＋原词受限查询

> 命名维护（2026-09-22）：本页名称映射：E/C-local＝SB-1.3，Q＝SB-1.3-exp01（选择侧实际基于SB-1.1）。历史分组、代码路径和运行编号保留。见[方法版本登记](../../../../research/方法版本登记表.md)。

运行：`followups_clocal_query_videomme50_20260915_r1`。本轮用户已确认实施，不提交、不推送。

## 问题与范围

- E：原C只修改最终重选，固定热点并集外粗帧；50最终问答＋50原范围分类。
- Q：原词受限改写，只输入原问题；50查询生成＋离线全片评分和B式选择，最终问答0次。
- 各类调用开始上限50，失败占额度，不重试生成。复用六组历史问答，不使用预留集。
- 不实施过滤、事件组合、λ解耦；不按新结果调参或扩展人工审计。

## 固定条件

开发50题、50视频、短/中/长17/17/16。LLaVA-Video-7B-Qwen2、官方源码`bce12e479bc4dfee2b9c50c88137b01ff51bd483`；BF16/SDPA、qwen_1_5、官方384、16唯一帧、实测3360视觉Token、原提示词/选项、贪心最多8新Token、种子2027。BLIP FP32/16帧批次、不截断，1FPS/0.25秒相位/真实PTS。

完整配置、查询提示词、白名单、执行代码/依赖/历史结果身份冻结在运行目录`protocol.json`及`code_snapshot/`。原方法文件和冻结审计不改写。

## E规则

沿用C分类、扫描、分段、竞争、热点和精确补查。热点并集端点包含，Fraction时间。仅收到新候选且有配额的段处理；窗口外原粗帧固定，窗口内粗帧数为该段可替换名额k，同段多窗口共享k。仅窗口内新旧候选竞争，去重参照包含同段固定帧与本次已选帧，分母计入固定数量。原公式、λ、0.2惩罚不变，PTS/编号同分排序，负增益填满。无新增或k=0保持粗选，配额与16帧不变。

先重放50冻结C候选池；正式E独立扫描原片。每题核对上游候选、λ/分类、分段、粗选、配额、热点和新增帧，浮点允许绝对1e-6、离散选择完全一致。不一致保留结果并暂停归因，不重答。

## Q规则与冻结评价

查询纯文本生成最多128新Token，不改变QA配置。英文词/数字/撇号规范化后，非固定白名单Token多重集合必须等于原问题。异常控制标记、选项式输出、多行/空/未正常结束均回退原题，无二次生成。语义忠实性单独review，不逐题润色。

50查询全部冻结后才评分。BLIP编码相同允许读取历史B分数/特征，显式标记缓存；变化查询完整扫描原片评分。λ复用历史B，新分数重新分段/竞争，无补查。

事前`proxy_manifest.json`固定42题75个r6 required/partial事实，排除491-2和321-2。每事实最佳见证排名、归一化排名与16帧保留；先题内平均再跨题平均。独立哨兵：103-1候选5，306-2候选88/89，409-3候选77/80，不混入主分母。

仅在语义无未解决漂移、宏平均保留改善、改善题多于退化题、B正确题未丢失原已保留必要事实全部见证时，建议后续另行确认查询版50题问答，不自动执行。

## 执行、计时与恢复

最多8空闲GPU、一卡长期模型进程；Torch算子和视频解码线程2，沿用旧运行Torch interop=1。E前5题验收不依正确率，余45按时长降序；E完成后查询生成/冻结，再Q评分。

`nohup + setsid`脱离会话、排他锁，调用开始及返回均原子落盘并fsync；不确定状态禁止再调用。完整输出跳过，已完成分类/答案可以从账本复用；恢复部分不混入首次处理时延。

E从分类到答案解析同步直接墙钟；必要运行检查点包含并单列。最终结果落盘、加载/预热/资产哈希/排队另报。Q不报告问答E2E。与历史C延迟只作不同批次和日志机制下的描述性比较。

## 验收与解释

新增窗口/配额/固定帧去重/负增益/查询约束/调用恢复测试，连同现有测试全部运行。每题16唯一帧及3360实际视觉Token、调用额度、历史哈希、恢复不重复核对。

E主对照C，次对照B及历史基线：正确数、分时长、Wilson区间、全量配对得失、分层bootstrap10000/2027；时延均值及P50/P90/P95和显存。Q只报告既定代理指标，不能推断准确率提升。

仅检查变化帧与既有事实，不重审4800帧。查询语义和差异review控制30分钟，不明项保留。E>C但E<B仅说明副作用可能减少；事实保留改善但准确率不变只报告机制改善；出现退化不在本轮调参。

## 命令

```bash
cd /home/admin/projects/explore_video_qa
nohup setsid /App/conda/envs/explore_video_qa/bin/python -u scripts/run_method_followups.py \
  --run-id followups_clocal_query_videomme50_20260915_r1 --phase all \
  --gpus 0,1,2,3,4,5,6,7 \
  > outputs/methods/followups_clocal_query_videomme50_20260915_r1.launcher.log 2>&1 < /dev/null &
```

同运行恢复增加`--resume`，指纹变化或不确定生成拒绝恢复。观察连接超时不代表任务停止，先读`status.json`/日志/PID，不重复启动。
