# Video-MME开发50题基线结果

2026-09-14完成。运行编号 `videomme50_uniform_topk_20260914_r1`。8张4090、专用Conda环境 `explore_video_qa`；100条均为本轮新测，旧单题验收未混入。

## 条件与完成情况

沿用默认条件：LLaVA-Video-7B-Qwen2、BF16＋SDPA、1 FPS／0.25秒相位候选、16个最终帧、官方384×384预处理、实际3360视觉Token、贪心最多8新Token。Top-K采用原问题BLIP-ITM正匹配概率、FP32、batch 16，无选项检索、MMR、分段或补查。两组独立读取原视频。

两组各50题，短／中／长17／17／16；视频总长14.14小时，实际初始候选50,903帧。前5题两组验收通过后自动完成其余45题。运行失败0、额外重试0、解析失败0、应用缓存命中0，未使用预留集。

调度器采用nohup＋setsid，PID/SID均为1429205，stdin为 `/dev/null`。实际起止约15:31:42–15:43:15 UTC，整批墙钟692.85秒（11.55分钟），包含约18.89秒资产预检、模型加载、合成预热、调度与保存。原 `execution.json` 的 `started_at_utc` 字段标记预检结束时刻；完整开始时间见 `pid.json`，直接墙钟总量包含预检。

每进程Torch CPU和候选扫描解码线程各2；最终16帧精确PTS重读沿用原入口单线程，两组一致。以下延迟是本轮多GPU并发观测值，不是隔离单卡微基准；未清空OS缓存。

## 正确率

| 时长 | 均匀采样 | Top-K |
|---|---:|---:|
| 短 | 11/17，64.7% | 10/17，58.8% |
| 中 | 11/17，64.7% | 10/17，58.8% |
| 长 | 7/16，43.8% | 9/16，56.2% |
| 总体 | **29/50，58.0%** | **29/50，58.0%** |

两组总体Wilson 95%区间均为44.2%–70.6%。Top-K减均匀的准确率差为0个百分点，分层配对bootstrap 95%区间为 **[−10，+10]个百分点**（10,000次，种子2027）。相同正确数不表示方法等效。

- 两组都对25题、都错17题。
- 仅均匀对4题：491-2、128-1、544-1、012-3。
- 仅Top-K对4题：383-2、893-1、693-1、260-2。

Top-K在长视频上多答对2题，短、中视频各少答对1题，净增益为0。这里仅描述本轮观测，不据此调整样本或参数。

## 性能

模型已加载，从原始视频读取到答案解析完成直接计时。一次性核验、加载、预热、排队和结果落盘不进入单题E2E。以下均为每题均值，单位秒。

| 阶段 | 均匀采样 | Top-K |
|---|---:|---:|
| 候选读取、解码与时间映射 | 29.462 | 29.662 |
| RGB转换 | 0 | 1.743 |
| BLIP预处理与传输 | 0 | 10.147 |
| BLIP前向评分 | 0 | 5.544 |
| 排名／索引选择 | 0.000173 | 0.000575 |
| 最终16帧读取／解码 | 1.588 | 1.582 |
| **选帧核心** | **0.000173** | **17.435** |
| **选帧全流程，含解码** | **31.053** | **48.710** |
| 问答整体 | 0.868 | 0.862 |
| **端到端** | **31.921** | **49.573** |

Top-K平均端到端增加17.652秒，约55.3%。额外成本主要来自BLIP预处理与评分；两组生成本身均约0.695秒。Top-K纯GPU评分吞吐183.62帧／秒，加入RGB转换、预处理与传输后58.39帧／秒。

| 指标 | 均匀采样 | Top-K |
|---|---:|---:|
| E2E P50(s) | 13.098 | 24.222 |
| E2E P90(s) | 84.730 | 136.159 |
| E2E P95(s) | 102.854 | 158.391 |
| 短视频E2E均值(s) | 4.965 | 6.340 |
| 中视频E2E均值(s) | 16.053 | 25.370 |
| 长视频E2E均值(s) | 77.422 | 121.223 |

两模型常驻约15.85 GiB；选帧阶段增量均值为均匀0、Top-K约0.715 GiB。两组工作进程每题峰值已分配显存均值均约20.17 GiB，不是均匀独立部署显存。完整分组分位数及显存统计见机器可读摘要。

## 证据与review

运行目录为 `outputs/baselines/videomme50_uniform_topk_20260914_r1/`。`summary.json`、`report.md`、`per_question.csv`为统计和逐题对照；`results/`保存100条原始结果；`protocol.json`、`code_snapshot/`和身份记录冻结执行条件；`attempts/`、`events.jsonl`、`workers/`保存调用和后台运行证据。

25项测试通过。`review.json`确认100条提示词、结果哈希、源PTS和视频覆盖正确，50组候选池一致，100次生成均返回。阶段计时核对通过，最大未归因开销0.438秒。完成后执行 `--resume`，确认跳过全部结果，没有新增生成。GPU已全部释放，本轮未提交Git或推送。

**阶段结论**：两组可追溯基准已建立。在本开发集上，Top-K没有总体准确率净提升，同时增加选帧和端到端耗时；研究方法尚未运行。这不是完整Video-MME成绩，也不能由50题证明稳定优势或等效。
