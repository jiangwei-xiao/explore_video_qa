# Qwen2.5-VL三策略Video-MME全量：固定LMMs-Eval协议

运行编号：`uniform_topk_rd12__qwen25vl__lmms_ref_v1__videomme2700__20260923__r01`。仅GPU 4–7；BASE-Uniform、BASE-BLIP-TopK、RD-1.2各2700次新回答，共8100次。LLaVA历史三组答案不重答，选帧/评分/补查算法不修改。

## 固定输入与协议

均匀直接复用`base_uniform__llava__lmms_ref_v1__videomme2700__20260923__r01`的原源帧均匀选择；Top-K/RD复用`topk_rd12__llava__lmms_ref_v1__videomme2700__20260923__r01`的冻结选择。每条输出保存对应来源文件哈希、源帧/PTS、原RGB哈希；重新精确解码并核验原RGB一致，不重新选帧、不二次采样。均匀/RD全部16帧；Top-K的133视频三题14帧、134视频三题11帧，保持不变。

| 项目 | 本轮固定值 |
|---|---|
| 数据 | 冻结Video-MME900视频/2700题，短/中/长各900题，原题/选项/计分不变 |
| 任务协议 | lmms-videomme-reference-v1 |
| LMMs-Eval | bb1ebe76e7a942386c25c4664f902e0e59e8a401 |
| WFS参考任务 | a424fc4528ecbe57edc93413826a2f2b8bb2c203，无字幕提示词/后缀/解析/聚合 |
| 模型 | /home/models/Qwen/Qwen2.5-VL-7B-Instruct，冻结非量化权重，只读 |
| 模型配置ID | qwen25vl-lmms-video-fa2-v1 |
| 环境 | explore_video_qa_lmms_qwen；Torch2.6.0、Transformers4.50.0、FA2 2.7.4.post1，完整环境另存 |
| 数值与生成 | BF16＋FlashAttention 2；贪心、batch1、最多16新Token、种子2027 |
| 媒体/对话 | 官方视频处理器＋官方chat template，非多图；实际经过框架Qwen2_5_VL.generate_until |
| 每帧像素 | 下限128×28×28=100352，上限768×28×28=602112；保持宽高比，28倍数 |
| 时间编码 | 固定FPS=2，非真实PTS间隔；不自定义MRoPE |
| 信息限制 | 无音频/外部字幕，不额外加入时长或逐帧时间文字；标准答案仅生成后计分 |

配置登记在`configs/lmms_videomme_qwen25vl_reference_v1.json`，不覆盖LLaVA配置。框架贪心分支实际传`do_sample=False, temperature=None, top_p=None`，逐题记录实际值；不启用随机采样。奇数帧由官方时间patch内部复制末帧补齐，11个唯一源帧→12编码位置，1→2；补齐不算新增证据。

逐题保存处理后尺寸、video_grid_thw、视觉Token、完整展开提示词与输入Token、真实生成Token和原始输出。Qwen视觉Token不套用LLaVA3360；真实PTS只用于取帧和日志，不提供给模型。跨模型同源RGB，不是等视觉Token或等位置编码实验，不宣称精确复现作者全部历史运行。

## 执行、检查点与资源

1. 先通过正式适配路径完成1/11/14/16帧合成验收，覆盖低分辨率、竖屏、横屏与像素上限；奇数自动补齐与显式复制末帧的处理器Tensor必须一致。合成答案正确性不作门槛。
2. 完整核验8100来源输入、900视频及Qwen权重字节哈希，冻结代码/环境/配置与数据身份；确认磁盘空间。正式运行只调用新框架适配，不能走旧Qwen的8 Token＋时间说明入口。
3. 先行001-1/2/3、002-1/2、133-1、134-1共7题×3方法=21条，计入8100预算；按输入/计时/生成和计分正确性放行，不按准确率放行。
4. 剩余题按视频时长降序派发。同题同物理GPU/同进程，方法顺序按冻结题序循环轮换；每卡常驻一个Qwen，不加载BLIP，Torch/解码线程各2。
5. 后台自动完成汇总，独立验收进程等待正式协调器结束后检查完整性及安全恢复，无需Codex继续发指令。

仅启动时使用4–7范围内实际空闲卡，运行中不自动扩卡。全忙时记录waiting_resources并等待，不抢占；部分题恢复必须等原GPU可用。`nohup＋setsid`、关闭stdin、日志重定向、排他锁、PID/SID与持久心跳。启动后关闭原SSH再连接核查进程存活；观察中断不重启。

调用前先写账本，每方法最多2700个ID。实际generate返回后立即落盘`raw_generations`（原始文本、Token、输入观测及生成状态），再由框架完成清理/计分。若只有后处理失败，可依据完整原始检查点恢复，不再生成；没有完整原始检查点或生成状态不确定则暂停。答错/解析失败不重试，解析失败计错；已开始失败占用额度，不自动追加预算。OOM/指纹或输入不符时暂停，不改精度、像素或帧数。

## 汇总与验收

三组各2700唯一题号、8100调用账本及原始生成检查点齐全；实际FA2、BF16、视频输入、补齐、尺寸、视觉Token及源RGB均核验。原任务聚合与独立正确数一致；完成恢复前后24300份结果/账本/原始生成文件不变，模型工作进程记录不增加。检查全部来源与5852个RD冻结文件不变；另做每方法10题有限RGB重解码，不冒充全量独立重解码。

报告总体/分时长准确率、Wilson区间；RD−均匀、RD−Top-K、Top-K−均匀采用按时长分层、以视频为簇的10000次配对bootstrap（种子2027），全部得失题号保留。单列开发200、其余2500、开发视频外2100和三方法均16帧的2694题。数据已被观察，不称未见盲测。

与LLaVA组成六组对照，方法收益在各模型内部计算。报告缓存选择读取、最终解码、带观测检查点的问答、显存与批次墙钟，均值/P50/P90/P95单列；加载、合成、失败和恢复成本单列。**本轮不是重新运行选帧的原片E2E**，不将BLIP和RD原算法成本算作零。

产物位于`outputs/full_videoqa/<运行编号>/`。入口`scripts/run_lmms_qwen_full.py`提供`--synthetic`、`--prepare-only`、正式运行及`--resume`；实际解释器固定`/App/conda/envs/explore_video_qa_lmms_qwen/bin/python`。独立CPU验收为`scripts/audit_lmms_qwen_full.py`，支持等待协调器PID后执行。

## 实施状态与时间

合成阶段曾因检查代码重用已展开视频提示词导致一次验收异常（1次合成生成、真实题0）；修正检查构造后通过4种帧数测试。随后预检发现依赖分发名`flash_attn`/`flash-attn`格式差异，修正核验键名而未升级依赖；代码重新验收后再冻结。全部合成尝试单独保存，不混入正式成绩。

四卡正式问答初估2–4小时，接入/预检45–90分钟，收尾20–40分钟；先行21条及累计100条后用真实吞吐更新ETA，不按速度或精度改变协议。只有三组完整且验收通过才称完成，不以超过均匀或论文分数为门槛。阶段有效成果review后提交，push另行授权。

2026-09-23 16:36 UTC进展：正式入口与合成输入验收通过，Qwen环境回归350项通过、1项既有跳过。4次最终合成检查的视觉Token为140/4320/5040/6144；全部合成尝试累计9次生成（含检查代码异常的1次），四个正式工作进程另各1次合成预热，共13次，真实题单独计数。8100份冻结来源、900视频与Qwen权重哈希核验完成，准备89.83秒。GPU 4–7后台PID/SID3660438，先行21条通过，累计201/8100，无正式失败；独立验收PID3664194。实际断开SSH再连接检查通过，同一进程继续运行。当前长视频阶段线性估计剩余约2.5小时，非完成时间保证；实际状态和ETA以运行目录记录为准，尚无正式全量成绩。

2026-09-23完成记录：18:34 UTC全量及汇总完成，18:39 UTC独立验收通过。三组各2700条新答案，均匀/Top-K/RD为58.11%/59.04%/59.89%；无正式失败/重试/解析失败，完成恢复未新增生成，旧结果/快照未变。四卡正式执行及汇总121.50分钟；六组对照、配对区间、子集与成本口径见[实验结果](实验结果.md)。上述已执行协议不按成绩修订。
